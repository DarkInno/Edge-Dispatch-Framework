FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /edge-agent ./cmd/edge-agent

FROM alpine:3.19
RUN apk add --no-cache ca-certificates
COPY --from=builder /edge-agent /edge-agent
EXPOSE 9090
ENTRYPOINT ["/edge-agent"]
